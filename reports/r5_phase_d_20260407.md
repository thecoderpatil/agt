# R5 Phase D: T+2 Reconciliation + Monitoring

Generated: 2026-04-07

## Delivered

### (a) T+2 Order Reconciliation — added to /reconcile

New "ORDER HEALTH" section in `/reconcile` output:
- Checks every `pending_orders` row with status FILLED/PARTIALLY_FILLED/SENT/ACKED/WORKING
- Matches against `master_log_trades` by `ib_order_id`
- Rows older than 2 days with no Flex trade match → flagged as T+2 orphans
- Output: count + first 3 orphan details

### (b) /orders Command — enhanced with timeline

`/status_orders` (wired as `/orders`) now shows:
- Status summary with counts and icons
- Last 10 orders with: ticker, action, strike, current status, IB order ID, fill price/qty
- Status timeline from `status_history` JSON (last 4 transitions)
- Stale staged order alert (>1 day)

### (c) Stale Order Detection

- `/reconcile`: reports stale staged orders in ORDER HEALTH section
- `/orders`: shows stale alert with ticker + age for orders staged >1 day

### (d) Command Deck "Recent Orders" Panel

New panel in right column (below Reconciliation):
- Shows last 10 orders from `pending_orders`
- Color-coded status: green (filled), sky (working), amber (staged), red (rejected/failed), grey (superseded)
- Displays ticker, action, status, age
- Query: `get_recent_orders()` in `queries.py`

## Tests: 74/74 passing (no new tests needed — display-only changes)

## Production DB: no new tables, no schema changes (Phase D is read-only monitoring)
