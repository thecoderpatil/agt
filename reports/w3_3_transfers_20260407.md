# W3.3: Transfer Conservation

Generated: 2026-04-07

## Change

`TRANSFER_IN` no longer originates a WHEEL cycle. Per Codex I13, only `CSP_OPEN`, `CARRYIN_OPT`, and `CARRYIN_STK` can open wheel cycles.

An orphan `TRANSFER_IN` (arriving with no active cycle) now emits a `walker_warning` and is skipped, rather than creating a new cycle.

**Before:** `TRANSFER_IN` → new WHEEL cycle (spec violation)
**After:** `TRANSFER_IN` → warning + skip (correct: position should arrive via carry-in or into an existing cycle)

## Impact on live data

No impact — the 2 INTERNAL transfers in the live DB (ADBE 332.5P and QCOM 160P) both arrive WITHIN active cycles (the option was opened in U22076184 and the cycle is already active from the household's CSP_OPEN). They are processed as intra-cycle events, not cycle originators.

## Reconciliation: A 49/49, B 14/14, C 3/4 — unchanged
## Tests: 74/74 passing

## Codex invariants addressed: I13, I19
