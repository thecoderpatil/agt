# MR !266 Ship Report — tripwire_burndown_mr2
**Date:** 2026-04-25
**Dispatch:** Tripwire Burn-Down MR 2 (paper_auto_execute fixture fix + 2 vestigial cases)
**Tier:** STANDARD (tests/** + .md only)

---

## DISPATCH: Tripwire Burn-Down MR 2
**STATUS:** applied

## FILES:
  tests/test_paper_auto_execute.py    +2/-6   net=-4
  tests/test_promotion_gates_paper_baseline.py  +1/-1  net=0
  tests/test_csp_allocator.py         +0/-3   net=-3
  TRIPWIRE_EXEMPT_REGISTRY.md         +6/-6   net=0

## COMMIT:
  squash: 23556840513c56a300044bcf6ccc0d26e2d969e5
  merge:  7bcde726ed30d5c1c32d866682ccc95b0f507032
  MR:     !266

## CI:
  pipeline: 2480013027  (branch; failed — 0 jobs, recurring runner SIGTERM infra issue, same as MR !264)
  STANDARD tier: poll once, log "CI pending — not blocking", proceed.
  post-merge main pipeline: 2480013390 (same infra issue, consistent with baseline)
  delta vs baseline: 0 (runner issue predates this MR)

## VERIFICATION:
  - loc_gate: GATE PASS (all 4 files within tolerance)
  - No agt_tripwire_exempt markers in test_paper_auto_execute.py, test_promotion_gates_paper_baseline.py, test_csp_allocator.py
  - monkeypatch.setenv("AGT_DB_PATH", ...) present at lines 68, 90 of test_paper_auto_execute.py
  - TRIPWIRE_EXEMPT_REGISTRY.md: 6 MIGRATED entries (3 from mr1, 3 from mr2)
  - Local pytest before push: paper_auto_execute 6/6 passed, promotion_gates 13xfail/7xpass, csp_allocator 3/3 passed

## LOCAL_SYNC:
  fetch/reset:     done (main @ 7bcde726)
  pip install:     no new deps
  smoke imports:   n/a (STANDARD tier, test-only changes)
  deploy.ps1:      skipped (STANDARD tier)
  heartbeats:      n/a

## NOTES:
  - test_paper_auto_execute root cause: staged_db/empty_db fixtures patched DB_PATH but not
    AGT_DB_PATH. Tripwire sets AGT_DB_PATH=sentinel; import telegram_bot calls
    _assert_canonical_db_path() → SelfHealingBootstrapError. Fix: setenv before setattr.
  - test_promotion_gates_paper_baseline: _SKIP_NO_PROD guard at collection time (module-scoped
    _prod_db_path() returns None in CI). Module-scoped prod_conn fixture pre-opens DB before
    function-scoped tripwire fixture runs — no conflict locally.
  - test_csp_allocator 3 tests: _prod_db_available() skipif guard + mocked _fetch_available_nlv
    in all 3 test bodies. Clean skip in CI, clean pass locally.
  - TRIPWIRE_EXEMPT_REGISTRY.md active exemptions now limited to Category 2 deck tests only
    (TestDeckQueries + TestDeckAuth — fix-by sprints FU-INTEGRATION-TEST-FIXTURES and
    FU-AGT-DECK-DI-REFACTOR, both unscheduled).
  - 9 total markers removed across this MR. Combined with MR !264 (3 exemptions), 12 of 12
    CI-included exemptions from the recon audit are now resolved.
