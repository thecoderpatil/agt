# R5 Phase C: Replace placeOrder Status Flip

Generated: 2026-04-07
Status: **Phase C complete — STOP for Yash review**

## Changes

### placeOrder site 1 — `/approve` handler (line ~6324)

**Before:**
```python
trade = ib_conn.placeOrder(contract, order)
conn.execute("UPDATE pending_orders SET status = 'approved' WHERE id = ?", (db_id,))
```

**After:**
```python
trade = ib_conn.placeOrder(contract, order)
ib_order_id = trade.order.orderId
ib_perm_id = trade.order.permId
conn.execute("UPDATE pending_orders SET ib_order_id = ?, ib_perm_id = ? WHERE id = ?",
             (ib_order_id, ib_perm_id, db_id))
append_status(conn, db_id, 'sent', 'placeOrder', {'ib_order_id': ..., 'ib_perm_id': ...})
```

### placeOrder site 2 — dynamic exit handler (line ~7830)

Changed `"status": "approved"` → `"status": "sent"` in the ticket payload. Added post-insert `ib_order_id`/`ib_perm_id` storage.

### placeOrder site 3 — match mid (line ~4242)

No change needed — this modifies an existing order's price, not a new order.

### Failure path (line ~6378)

Changed direct `status = 'failed'` write to `append_status(conn, db_id, 'failed', 'placeOrder_exception', {'error': ...})`.

### `/status_orders` display (line ~6643)

Updated icon mapping: `sent`/`acked`/`working` → green check, `filled`/`partially_filled` → heavy check.

## Audit: zero remaining 'approved' writes

`grep "'approved'" telegram_bot.py` returns 0 hits outside comments and the ORDER BY display case.

## Status flow now

```
STAGED ─→ PROCESSING ─→ SENT ─→ [event-driven from here]
                              ↓
                    orderStatusEvent: ACKED → WORKING
                    execDetailsEvent: FILLED / PARTIALLY_FILLED
                    orderStatusEvent: CANCELLED / REJECTED
                    exception:        FAILED
```

## Tests: 11 order state tests, 74 total passing

| Test | Validates |
|------|-----------|
| test_end_to_end_filled_lifecycle | staged → processing → sent → acked → working → filled |
| test_reject_lifecycle | staged → processing → sent → rejected |
| test_cancel_lifecycle | staged → processing → sent → acked → cancelled |
| test_partial_fill_lifecycle | sent → acked → working → partially_filled → filled |
| (7 prior tests) | Basic transitions, terminal guard, monotonic, mapping, backfill, orphan |

## No changes to order sizing, pricing, or contract selection

Phase C is purely status-tracking. The `placeOrder()` call itself, the contract construction, and the order parameters are UNCHANGED.

## STOP — awaiting Yash review before Phase D.
