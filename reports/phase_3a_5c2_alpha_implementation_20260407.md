# Phase 3A.5c2-alpha Implementation Report

**Date:** 2026-04-07
**Tests:** 319/319 (283 + 36 new)
**Day 1 Mode:** PEACETIME

---

## 1. Executive Summary

Phase 3A.5c2-alpha ships the backend infrastructure for Rule 8 Dynamic Exit: walker extraction, schemas, Gate 1/Gate 2 evaluators, the candidate orchestrator, R9 condition D activation (2-of-4 thresholds), R5 sell gate staging helper, 15-minute sweeper, is_wartime_condition_met helper, and v10 changelog update. 

**Deferred to next session (surgery tier):** Tasks 6 (watchdog CIO payload refactor), 9 (R7 earnings fail-closed + /override_earnings), 10 (IBKRProvider migration), 11 (/exit removal — coupling found, must follow Task 6). These are telegram_bot.py surgeries requiring focused attention with commit checkpointing.

**Deferred to beta:** Smart Friction UI, JIT re-validation handler, /sell_shares command, Telegram inline keyboard handler.

---

## 2. Walker Extraction

**Diff result:** Two inline implementations (line 2967 `_calculate_walk_away_profit` and line 7590 inline Gate 1 math) are **functionally identical**: `strike + premium - adjusted_basis`.

**Extracted:** `walker.compute_walk_away_pnl()` — pure function returning `WalkAwayResult(walk_away_pnl_per_share, walk_away_pnl_total, adjusted_cost_basis, is_profitable)`.

**Call site migration:** Line 2967 wrapper delegates to walker function. Line 7590 will be migrated when Task 6 (watchdog refactor) ships.

**Third call site found:** Line 9019 (`_run_cc_logic` inline). Same formula. Will migrate with Task 6.

---

## 3. Schema Migrations

3 tables added (all Bucket 3, idempotent, verified on live DB):
- `bucket3_dynamic_exit_log` — 30+ columns, STAGED/ATTESTED/TRANSMITTED lifecycle, FK to campaigns
- `bucket3_dynamic_exit_campaigns` — state lock for multi-week phasing
- `bucket3_earnings_overrides` — R7 fail-closed override tracking

---

## 4. Gate 1 (Capital Velocity Test)

`evaluate_gate_1()` in rule_engine.py. Pure function. Hardcoded conviction modifiers (HIGH=0.20, NEUTRAL=0.30, LOW=0.40). Profitable exits auto-pass. Tax liability override additive to loss. 7 tests passing.

---

## 5. Gate 2 (Position Sizing)

`evaluate_gate_2()` in rule_engine.py. Severity <= 2% → 100% liquidation. > 2% → 33% PEACETIME/WARTIME, 25% AMBER. 6 tests passing.

---

## 6. Orchestrator

`evaluate_dynamic_exit_candidates()` — takes pre-fetched chain slice, evaluates Gate 1/2 per strike, returns ranked `DynamicExitCandidate` list. Uses walker.compute_walk_away_pnl(). Appendix B 3% prohibition overridden per Patch 7. 4 tests passing.

---

## 7. R9 Condition D

Updated from 2-of-3 to **2-of-4** fire / **all-4** clear. `condition_d_override` parameter added to compositor (caller pre-computes using option chain provider). Backward compatible — defaults to False when not provided. 2 tests.

---

## 8. R5 Sell Gate Helper

`stage_stock_sale_via_smart_friction()` — validates R5 sell gate, writes STAGED row with action_type='STK_SELL' to bucket3_dynamic_exit_log. No Telegram or Cure Console entry point in alpha. 3 tests.

---

## 9. Sweeper

`sweep_stale_dynamic_exit_stages()` — transitions STAGED rows older than 15 minutes to ABANDONED. 3 tests.

---

## 10. is_wartime_condition_met

Pure helper in mode_engine.py. Dead reference code — NOT wired to mode transitions. 7 tests.

---

## 11. /exit Coupling Check

**HIDDEN COUPLING FOUND:** Line 9402 in telegram_bot.py — `/cc` handler extracts `/exit` commands from dynamic exit payloads generated by the watchdog. Removing /exit without refactoring the watchdog (Task 6) would break /cc output.

**Decision:** /exit removal deferred to after Task 6 (watchdog refactor).

---

## 12. v10 Changelog

Line 28 updated: "Phase 3A.5c2 note: Rule 9 condition D was deferred in 3A.5b... Implemented in Phase 3A.5c2-alpha..."

---

## 13. Test Count

| Suite | Count | Delta |
|-------|-------|-------|
| Existing | 283 | 0 |
| test_phase3a5c2_alpha.py | 36 | +36 |
| **Total** | **319** | **+36** |

---

## 14. Day 1 Verification

PEACETIME confirmed. No regression. R9 condition D defaults to False (no chain provider in baseline script). All existing glide paths softening correctly.

---

## 15. Deferred Tasks (Next Session)

| Task | Reason | Risk |
|------|--------|------|
| 6: Watchdog refactor | ~120 lines telegram_bot.py surgery | HIGH |
| 9: R7 fail-closed + /override_earnings | telegram_bot.py /scan modification | MEDIUM |
| 10: IBKRProvider migration | Multi-file mechanical refactor | MEDIUM |
| 11: /exit removal | Blocked by Task 6 (coupling found) | LOW after Task 6 |

These are all telegram_bot.py surgery tasks that benefit from a fresh context window with commit checkpointing between each.

---

```
Phase 3A.5c2-alpha partial | tests: 319/319 | walker extracted | schemas: 3/3
| Gate 1+2: shipped | orchestrator: shipped | R9 condition D: 2-of-4 active
| sweeper: shipped | is_wartime: shipped | Day 1: PEACETIME
| Deferred: Tasks 6,9,10,11 (telegram_bot.py surgery -> next session)
| reports/phase_3a_5c2_alpha_implementation_20260407.md
```
