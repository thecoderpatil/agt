# MR !271 Ship Report — Sprint 13: loc_gate hardening + operator_interventions CHECK + heartbeat retention

**Date:** 2026-04-26
**MR:** !271
**Branch:** sprint-13-followups
**Squash SHA:** 7b7ac2e9bf838c2966064756b877e8004bd2c721
**Merge commit:** 97014ba8730b38c5fbfa89a42783d676be3ae5d6
**Tier:** CRITICAL

---

## DISPATCH: Sprint 13 — loc_gate hardening + operator_interventions CHECK + heartbeat retention
## STATUS: applied

## FILES

| Path | Delta | Notes |
|------|-------|-------|
| `scripts/precommit_loc_gate.py` | +12/-0 | AnnAssign branch + id_strings field/parse/validate |
| `scripts/migrate_operator_interventions_kind_check.py` | +151/-0 | New — CHECK constraint migration |
| `scripts/migrate_heartbeat_samples_archive.py` | +39/-0 | New — archive table migration |
| `agt_scheduler.py` | +44/-0 | _heartbeat_archive_job + add_job + registered.append |
| `tests/test_sprint13_design.py` | +76/-0 | New — 4 tests |
| `tests/test_agt_scheduler.py` | +2/-0 | heartbeat_archive added to test_register_jobs_a5e_set |
| `.gitlab-ci.yml` | +1/-1 | Added test_sprint13_design.py to pytest line |
| `reports/sprint_13_dispatch_20260426.md` | +204/-0 | Dispatch doc committed to git |

## COMMIT

```
squash: 7b7ac2e9bf838c2966064756b877e8004bd2c721
merge:  97014ba8730b38c5fbfa89a42783d676be3ae5d6
MR:     !271
```

## CI

```
pipeline: 2480974794  (post-merge main)
result:   1458 passed / 6 failed / 1 skipped / 8 deselected
delta vs baseline (1454): +4 passed (test_sprint13_design.py — 4 new tests)
pre-existing failures: test_news_adapters (6, API-key, unchanged)
```

---

## MIGRATION_LOG

### migrate_operator_interventions_kind_check.py --apply

```
operator_interventions: 0 rows to migrate
Planned DDL:
CREATE TABLE operator_interventions (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    occurred_at_utc  TEXT NOT NULL,
    operator_user_id TEXT,
    kind             TEXT NOT NULL CHECK(kind IN ('reject','reject_rem','approve','recover_transmitting','flex_manual_reconcile','halt','direct_sql','manual_terminal','restart_during_market')),
    target_table     TEXT,
    target_id        INTEGER,
    before_state     TEXT,
    after_state      TEXT,
    reason           TEXT,
    notes            TEXT
)
Step 1: VACUUM INTO backup...
  backup: C:\AGT_Telegram_Bridge\backups\agt_desk_20260426_220004_pre_kind_check.db
Step 2: Pre-migration integrity_check...
  result: ok
Step 3: Applying table-recreate migration...
  migrated 0 rows
Step 4: Post-migration integrity_check...
  result: ok
Snapshot written: reports\mr_sprint13_post_migration_table_info.json
DONE: CHECK constraint added to operator_interventions
```

### migrate_heartbeat_samples_archive.py

```
DONE: daemon_heartbeat_samples_archive created
```

Note: Migration ran against `C:\AGT_Telegram_Bridge\agt_desk.db` (prod DB symlinked as default). Services were stopped during migration. Post-migration, deploy.ps1 first attempt failed smoke (agt_scheduler heartbeat stale due to accumulated manual-stop time before deploy.ps1 ran). Deploy.ps1 auto-rolled back cleanly. Second deploy attempt with services in live/fresh state: smoke PASS, exit 0.

---

## BACKUP_PATH

`C:\AGT_Telegram_Bridge\backups\agt_desk_20260426_220004_pre_kind_check.db`

Pre-deploy backup also at: `C:\AGT_Runtime\backups\agt_desk_20260426_180234_pre_deploy.db` (13.28 MB)

---

## TABLE_INFO_SNAPSHOT

Contents of `reports/mr271_post_migration_table_info.txt`:

```
0|id|INTEGER|0||1
1|occurred_at_utc|TEXT|1||0
2|operator_user_id|TEXT|0||0
3|kind|TEXT|1||0
4|target_table|TEXT|0||0
5|target_id|INTEGER|0||0
6|before_state|TEXT|0||0
7|after_state|TEXT|0||0
8|reason|TEXT|0||0
9|notes|TEXT|0||0
CREATE TABLE "operator_interventions" (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    occurred_at_utc  TEXT NOT NULL,
    operator_user_id TEXT,
    kind             TEXT NOT NULL CHECK(kind IN ('reject','reject_rem','approve','recover_transmitting','flex_manual_reconcile','halt','direct_sql','manual_terminal','restart_during_market')),
    target_table     TEXT,
    target_id        INTEGER,
    before_state     TEXT,
    after_state      TEXT,
    reason           TEXT,
    notes            TEXT
)
```

"CHECK" confirmed present in live sqlite_master DDL.

---

## CHECK_CONSTRAINT_VERIFICATION

```python
& python -c "
import sqlite3
conn = sqlite3.connect('C:/AGT_Runtime/state/agt_desk.db')
try:
    conn.execute(\"INSERT INTO operator_interventions (occurred_at_utc, kind) VALUES ('2099-01-01T00:00:00', 'INVALID_KIND')\")
    conn.commit()
    print('FAIL: IntegrityError NOT raised')
except sqlite3.IntegrityError as e:
    print('PASS: CHECK constraint active:', e)
finally:
    conn.rollback()
    conn.close()
"
```

**Result:** `PASS: CHECK constraint active: CHECK constraint failed: kind IN ('reject','reject_rem','approve','recover_transmitting','flex_manual_reconcile','halt','direct_sql','manual_terminal','restart_during_market')`

---

## ARCHIVE_TABLE_VERIFICATION

```sql
SELECT sql FROM sqlite_master WHERE name='daemon_heartbeat_samples_archive';
```

Result:
```sql
CREATE TABLE daemon_heartbeat_samples_archive (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    daemon_name TEXT NOT NULL,
    beat_utc    TEXT NOT NULL,
    pid         INTEGER NOT NULL,
    client_id   INTEGER,
    notes       TEXT,
    archived_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP
)
```

Row count: 0 (first archive run is Sunday 2026-05-03 03:00 ET).

---

## HEARTBEAT_ARCHIVE_JOB_REGISTERED

From `C:\AGT_Telegram_Bridge\logs\nssm_agt-scheduler_stderr.log` (both boots):

```
2026-04-26 18:00:53,594 [INFO] agt_scheduler: Registered 18 job(s): ['heartbeat_writer', 'orphan_sweep', 'attested_sweeper', 'el_snapshot_writer', 'beta_cache_refresh', 'beta_startup', 'corporate_intel_refresh', 'corporate_intel_startup', 'flex_sync_eod', 'flex_sync_retry_poller', 'universe_monthly', 'conviction_weekly', 'flex_sync_watchdog', 'flex_sync_zero_row_check', 'terminal_state_sweeper', 'phase_b_proof_final', 'phase_b_proof_preview', 'heartbeat_archive']
```

`heartbeat_archive` confirmed at position 18 (last) in registered list.

---

## LOCAL_SYNC

```
fetch/reset:     done (97014ba Merge branch 'sprint-13-followups' into 'main')
pip install:     no new deps
smoke imports:   n/a (deploy.ps1 handles smoke)
deploy.ps1:      exit 0 (second attempt after first rollback due to accumulated stop time)
                 backup: C:\AGT_Runtime\backups\agt_desk_20260426_180234_pre_deploy.db (13.28 MB)
                 integrity_check: ok
                 smoke: HEARTBEAT_OK, AGT_DB_PATH: OK
heartbeats:      agt_bot=2s  agt_scheduler=9s  (post-deploy)
```

---

## VERIFICATION

- `ast.parse`: all 4 new/modified .py files passed pre-commit
- precommit_loc_gate: PASS (all 6 files within tolerance)
- local pytest `tests/test_sprint13_design.py`: 4/4 PASS
- Sentinel greps: all required_sentinels confirmed present in each file
- Post-merge CI delta: +4 passed (exact target)

---

## NOTES

1. **First deploy.ps1 attempt rolled back** — accumulated stop time (services manually stopped before deploy.ps1 ran) caused agt_scheduler heartbeat to show 136s stale at t+60s smoke check. Auto-rollback was clean. Second attempt with services live: smoke PASS. No data loss, no user-visible impact.

2. **Migration ran on DB with 0 rows** — `operator_interventions` was empty at migration time. Table-recreate was clean. CHECK constraint active and verified.

3. **test_register_jobs_a5e_set** — required a fix commit to add `"heartbeat_archive"` to the hardcoded ordered list. Added as separate commit `7b7ac2e9` on sprint-13-followups before merge.

4. **ADR backlog A+B+E** — all confirmed shipped in prior sprints per dispatch. No carry-forward.

## KNOWN_DEFERRALS

- `telegram_bot.py` bot-side attested-sweeper deferred at line ~21858 (Sprint 12 P1 deferral, A5e cutover)
- `agt_equities/rule_engine.py` `sweep_stale_dynamic_exit_stages` Option B refactor deferred
