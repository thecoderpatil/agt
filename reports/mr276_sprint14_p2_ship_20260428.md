# MR !276 Ship Report — Sprint 14 P2: CSP Per-Ticker Approval Gate

**Date:** 2026-04-28  
**MR:** !276  
**Branch:** sprint-14-p2-approval-gate  
**Squash SHA:** 07f24cb68af7  
**Merge SHA:** 9a4d8727db0f  

## Changes

Five files shipped:

1. `scripts/migrate_sprint14_p2_approval_gate.py` (NEW) — creates `csp_ticker_approvals`, extends `operator_interventions.kind` CHECK with 4 new CSP kinds (13 total).
2. `agt_equities/csp_digest/formatter.py` — rename `csp_approve:`/`csp_reject:` callbacks to `cta_approve:`/`cta_reject:` (incl. `_all` variants); remove paper-mode early `[]` return so paper digests emit audit-trail buttons.
3. `csp_digest_runner.py` — ticker dedup in `build_digest_payload`; `CSP_TICKER_APPROVAL_TIMEOUT_MINUTES = 30`; `_record_live_ticker_approvals` on LIVE digest send.
4. `agt_scheduler.py` — `_csp_timeout_sweeper_job` cron at 10:10 ET Mon-Fri; reads `AGT_CSP_TIMEOUT_DEFAULT`; logs to `operator_interventions`.
5. `telegram_bot.py` — `_auto_execute_staged` LEFT JOIN gate on `csp_ticker_approvals`; new `handle_csp_ticker_callback` for `cta_` buttons with immediate execute on approve.

Test updates: `test_agt_scheduler.py`, `test_paper_auto_execute.py`, `test_csp_digest_formatter.py`.

## CI

```
pipeline: 2486539414   6 failed / 1462 passed / 1 skipped / 8 deselected
delta vs baseline:     +0 new failures  (6 pre-existing test_news_adapters)
```

CI passes. The 6 failures are pre-existing test_news_adapters failures unrelated to this MR.

## LOCAL_SYNC

```
fetch/reset:     done  HEAD=9a4d872
pip install:     no requirements.txt (dep-free MR)
smoke imports:   agt_scheduler OK, formatter OK, csp_digest_runner OK
deploy.ps1:      exit 0  backup=agt_desk_20260428_164948_pre_deploy.db  smoke=pass
heartbeats:      agt_bot=27s  agt_scheduler=2s
```

## Migration

```
script:          scripts/migrate_sprint14_p2_approval_gate.py --apply
backup:          C:\AGT_Telegram_Bridge\backups\agt_desk_20260428_205228_pre_p2_approval_gate.db
integrity_pre:   ok
integrity_post:  ok
cta_table:       CREATED  (with UNIQUE idx on run_id+ticker, status+timeout_at_utc index)
oi_check:        EXTENDED to 13 kinds
oi_rows:         0 (migrated atomically via tx_immediate recreate)
migrated_at_utc: 2026-04-28T20:52:28.931576+00:00
```

## Verification

```
csp_ticker_approvals DDL:    CREATE TABLE confirmed, status CHECK, timeout_at_utc NOT NULL
UNIQUE index:                idx_cta_run_ticker ON (run_id, ticker)
operator_interventions:      13 kinds in CHECK constraint
new kinds INSERT test:       csp_ticker_approve OK
                             csp_ticker_reject OK
                             csp_timeout_auto_approve OK
                             csp_timeout_auto_reject OK
invalid kind reject test:    IntegrityError raised (CHECK enforced)
cta_ callback sentinels:     cta_approve: present in formatter.py ✓
                             csp_timeout_sweeper job id present in agt_scheduler.py ✓
                             handle_csp_ticker_callback present in telegram_bot.py ✓
```

## Notes

- Migration ran after deploy.ps1 so services restarted with new code before schema was applied — window was ~3 minutes. `_auto_execute_staged` gate is additive (passes all staged rows when `csp_ticker_approvals` is absent); no orders in flight during this window.
- CI pipeline was blocked by RTH window (09:30–16:00 ET) all day; triggered at 16:45 ET post-market.
- `AGT_CSP_TIMEOUT_DEFAULT` not yet set in NSSM env — sweeper will default to `auto_reject` at first 10:10 ET fire. Architect to confirm desired default for paper vs live.
