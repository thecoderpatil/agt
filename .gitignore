# Secrets
.env
.env.*
*.env
.deck_token
litestream.yml
litestream.exe

# Databases
*.db
*.db-wal
*.db-shm
*.db.phase*_baseline_*
*.backup

# IBKR Flex XML fixtures (contain account data)
tests/fixtures/master_log_*.xml
*flex*.xml

# Archive (may contain old credentials, CSVs with account data)
Archive/

# Audit bundles
audit_bundles/

# Test framework artifacts
.hypothesis/

# Data files with account info
data/inception_carryin.csv

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Dashboard output
dashboard_output/

# Litestream WAL segments
.agt_desk.db-litestream/

# Unknown archives
files.zip

# Claude Code local settings
.claude/

# Superseded rulebook versions (v9 is canonical)
Portfolio_Risk_Rulebook_v6.md
Portfolio_Risk_Rulebook_v7.md
Portfolio_Risk_Rulebook_v8.md

# ADR-007: prose directive retired, machine-readable incidents table replaces it
_WEEKLY_ARCHITECT_DIRECTIVE.md

# MR !84: hygiene expansion so NO_LOCAL_DRIFT does not fire on operational noise.
# Reports are transient; pre-commit scaffolding scripts are temporary.
reports/
scripts/_*.py
scripts/_*.txt
scripts/_*.sh
*.diff
*.patch
.coverage
coverage.xml
htmlcov/

# Windows operator scratch
bot_output.txt
bot_output_*.log
*.log.tmp

# Transcripts / local notes
.claude-cowork-notes.md.bak

# MR !84: Cowork / session / ops scratch that NO_LOCAL_DRIFT shouldn't fire on.
# These accumulate during active desk work and must stay uncommitted
# (secrets, session state, one-shot debug runners). Do not commit any of
# these; add new patterns here if NO_LOCAL_DRIFT flags ops noise.

# Windows bot lifecycle / pid files
.bot.pid

# Session notes (live + agent scratch)
.claude-cowork-notes.md
desk_state.md

# Secrets outside .env (token + recovery codes).
.gitlab-token
gitlab-recovery-codes.txt

# Local caches / experiment dirs
agt_desk_cache/
hardening/

# Repo-root scratch docs from agent sessions
/_*.md

# Transient test sentinel
test_write.tmp

# Multi-agent sync architecture — 2026-04-19 PM-19f
# Coder runs in a linked worktree at .worktrees/coder.
# Architect runs in the main worktree. Do not track worktree directories.
.worktrees/

# PM-19g hook audit log — local-only, do not track
.claude/hooks/audit.jsonl
