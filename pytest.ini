[pytest]
norecursedirs = Archive .venv audit_packages tmp_before architect_kb_day2 .git .hypothesis
markers =
    sprint_a: Decoupling Sprint A targeted CI test set (ib_conn, agt_scheduler, health, flex_sync_atomic). Selected via `pytest -m sprint_a` in .gitlab-ci.yml.
    hardening: Trading-logic hardening sprint tests (2026-04-15). Deselected from default CI; run via `pytest -m hardening`.
