"""ADR-014 synthetic data + evaluation harness.

Submodules:
    bates_fft — FFT option pricer under Bates dynamics.
    schemas   — Pydantic v2 models for Bates params + eval results.

MC harness, calibration, fidelity metrics ship in subsequent MRs
(MR-D.1 through MR-D.7 per ADR-014 §10).
"""
