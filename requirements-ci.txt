# AGT CI test dependency pins. Sourced via PyPI latest-stable at time of
# pinning (2026-04-14). Edit deliberately — bumping any line forces a
# cache-key-invalidating rebuild in .gitlab-ci.yml.
pytest==9.0.3
pytest-asyncio==1.3.0
hypothesis>=6.150,<7
apscheduler==3.11.2
pytz==2026.1.post1
ib_async==2.1.0
python-dotenv==1.0.1
