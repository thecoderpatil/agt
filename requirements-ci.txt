# AGT CI test dependency pins. Sourced via PyPI latest-stable at time of
# pinning (2026-04-14; expanded 2026-04-15 for tests that import
# telegram_bot). Edit deliberately — bumping any line forces a
# cache-key-invalidating rebuild in .gitlab-ci.yml.
pytest==9.0.3
pytest-asyncio==1.3.0
hypothesis>=6.150,<7
apscheduler==3.11.2
pytz==2026.1.post1
ib_async==2.1.0
python-dotenv==1.0.1

# Expanded 2026-04-15: required so tests that import telegram_bot
# (test_b2_cc_fill_permid_retry, test_inception_delta_fill) can run in
# CI. Scope is the top-level telegram_bot import graph only; transitives
# are pulled by pip.
nest_asyncio==1.6.0
anthropic==0.39.0
finnhub-python==2.4.20
pandas==2.2.3
yfinance==0.2.50
python-telegram-bot==21.7
